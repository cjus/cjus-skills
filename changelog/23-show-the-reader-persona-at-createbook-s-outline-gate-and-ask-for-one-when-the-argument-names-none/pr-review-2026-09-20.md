# Review, 2026-09-20 — PR #33

Verdict: **APPROVE**. No critical findings. Two important, three suggestions,
two deferred. All four items taken are fixed in `HEAD`; the disposition of
every finding is below.

## Fixed

**🟡 The provenance marker was binary, but Phase 2 created a third origin.**
Before this branch a persona arrived one of two ways: named in the argument, or
inferred. Phase 2 added a third — the operator's answer to the new step 1 ask —
and the gate wording had no label for it. An agent following the text literally
would have reported "inferred" for a persona the operator had just dictated,
which is a false alarm at the one gate built to raise real ones, or "from the
argument", which is untrue. The two halves of the branch were each correct and
the gap sat exactly at their intersection. `SKILL.md § 3` now names all three
origins, its following paragraph leads on "The third case" rather than "The
inferred one", and `README.md` matches.

**🟡 `NOTES.md` asserted something the diff does not do.** The entry claimed
"Both step 1 paragraphs say so explicitly" about the persona/posture boundary.
Only the second does; the ask paragraph never mentions it. `NOTES.md` is the
file whose stated purpose is that its claims survive checking, and its own
§ The rule most worth keeping is about exactly this — a claim widened past what
was verified. Reworded to name the one paragraph that does it, and the
incidental "one line above" dropped, the gap being a paragraph rather than a
line.

**🟢 "always" was hardened past the evidence.** `PLAN.md` says a plausible
persona can be inferred "nearly always"; `SKILL.md` and `NOTES.md` both wrote
"always". Not a deliberate strengthening — carried over unnoticed. The argument
does not need the absolute. Both now read "almost always".

**🟢 `SKILL.md`'s Arguments table had fallen behind the README.** Its
`--source`, `--minutes` and `--no-tags` rows each cite their section; the first
row said "where useful the reader" and pointed nowhere, while the README had
been updated to mention the ask. The row now carries "Name no reader and the
skill asks for one; see § 1."

## Deferred

**Provenance is never written down.** `SKILL.md § 2` records the reader at the
top of `OUTLINE.md` but not where it came from, so § 3 recalls the origin
rather than reading it, and a run resumed between the two steps loses it.
A real improvement, but it changes what § 2 records and this branch does not
otherwise touch § 2. → `PLAN.md § Deferred`.

**Step 5's prompt hands a chapter agent "the reader" (persona) while the file
that agent then reads calls the posture "The reader".** The collision is named
only at step 1, which no chapter agent reads. Pre-existing on `main` and not
worsened here. → `PLAN.md § Deferred`.

**Dropped:** batching step 1's three asks into one message. The reviewer raised
it and recommended dropping it; agreed, the asks fire on different conditions.

## Reviewer's two questions, answered

**Should an inferred persona prompt for its own confirmation, or only be
stated?** Only stated. Deliberate: § 3 already ends "Stop and wait", so the
operator cannot pass the persona without passing the gate it sits in. A second
confirmation inside the one blocking gate would make the gate two gates, and
the skill has exactly one on purpose.

**Was "always" deliberate?** No. Carried over unnoticed, and fixed above.
